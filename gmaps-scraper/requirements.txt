# Google マップ営業リスト自動抽出システム - 依存パッケージ
# インストール: pip install -r requirements.txt
# Playwright ブラウザ初期化: playwright install chromium

# ブラウザ自動操作
playwright>=1.40.0

# ヘッドレスブラウザのフィンガープリント軽減（任意、ないと警告のみ）
# playwright-stealth>=1.0.6

# WHOIS照会
python-whois>=0.9.4

# データ加工
pandas>=2.1.0

# Streamlit UI
streamlit>=1.29.0

# YAML 設定ファイル
PyYAML>=6.0.1

# Supabase（.env に SUPABASE_URL/KEY を設定した場合のみ使用）
supabase>=2.3.0

# .env ファイル読み込み
python-dotenv>=1.0.0

# Google スプレッドシート（credentials.json を配置した場合のみ使用）
gspread>=6.0.0
google-auth>=2.0.0
